# Network Module

Planned responsibility:

- VPC
- public/private subnet contract
- route tables
- security-group primitives

Important first-slice rule:

- `NAT Gateway` is disabled by default unless a later implementation step proves it is required
