apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: inference-gateway-mock
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
